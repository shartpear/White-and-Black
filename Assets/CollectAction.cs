using UnityEngine;
using System.Collections;

public class CollectAction : BeingAction {
		Resource resource;
	
	/*public void init(Tile tile,Being b, Resource r){
		name="Collect";
		this.agent=b;
		this.target=tile;
		this.resource=r;
	}
	//this is called from the villager update method
	//returns another action to drop the resources off at the village
	public BeingAction act(){
		if(((Tile)target).contains(agent.transform.position)){//if on the tile, move the resources
			((Tile)target).collectResorce(resource);
		}else{//move the agent onto the tile
			
		}
	}*/
}
